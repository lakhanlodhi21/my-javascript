// Windows method 
// BOM(Browser object model) = intraction with the browser window

function smart() {

    // window.alert("Hello i am lakhan ") // alert
    // window.confirm("are u ok?")
    // let confirmans = window.confirm("are u ok?") // confirm
    // console.log(confirmans);
    // let a = window.prompt(" What is your name") // promt
    // window.alert(a)
    let ans = confirm("do you like js")
    if (ans) {
        
        alert("thanks")
    }
    else {
        alert(" jaa yrr bhai ")
    }

}
//smart()
